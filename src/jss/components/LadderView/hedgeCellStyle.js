import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  profit: {
    color: '#1ae17d',
  },
  loss: {
    color: '#710909',
  },
  breakEven: {
    color: '#000',
  },
}));

export default useStyles;
