import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  profit: {
    color: '#1ae17d',
  },
  loss: {
    color: '#ff7a7a',
  },
  breakEven: {
    color: '#000',
  },
}));

export default useStyles;
