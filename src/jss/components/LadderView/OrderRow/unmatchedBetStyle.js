import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  cancelButton: {
    height: '15px',
    float: 'right',
    marginRight: '2px',
  }
}));

export default useStyles;
